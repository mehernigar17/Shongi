import 'package:flutter/foundation.dart'; import '../repositories/statistics_repository.dart'; import '../models/statistics_data.dart'; class StatisticsViewModel extends ChangeNotifier { StatisticsViewModel(this._repository); final StatisticsRepository _repository; int _selectedRange=0; StatisticsData? _data; bool _isLoading=false; int get selectedRange=>_selectedRange; StatisticsData? get data=>_data; bool get isLoading=>_isLoading; Future<void> selectRange(int value) async {_selectedRange=value;_isLoading=true;notifyListeners();try{_data=await _repository.load(value);}finally{_isLoading=false;notifyListeners();}} }
